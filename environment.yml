name: phidot
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10.9
  - pip
  - pip:
    - -r requirements.txt
    - -r requirements-dev.txt
    - -e ./space-stream
prefix: /opt/miniconda3/envs/phidot
